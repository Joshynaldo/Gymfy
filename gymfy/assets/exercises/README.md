# Exercise GIFs

Drop one animated GIF per exercise here, named after the exercise's `id`
(the slug used in `lib/features/exercises/data/exercise_seed_data.dart`),
e.g. `barbell_bench_press.gif`.

Any exercise without a matching GIF shows a "Preview coming soon" placeholder
on its detail screen — so it's fine to add these gradually.
